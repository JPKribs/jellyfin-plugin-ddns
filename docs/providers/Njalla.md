# Njalla

Set the record's **Provider** to **Njalla**, then fill these fields.

- **Hostname**: the record.
- **Password**: the per record dynamic DNS key. Njalla shows a DDNS key for each record.

_Ported from ddclient's `nic_njalla_update`._
