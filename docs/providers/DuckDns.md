# Duck DNS

Set the record's **Provider** to **Duck DNS**, then fill these fields.

- **Hostname**: your Duck DNS subdomain label, such as myhost. Comma separate several.
- **Password**: your Duck DNS token, shown at the top of duckdns.org after you sign in.

_Ported from ddclient's `nic_duckdns_update`._
